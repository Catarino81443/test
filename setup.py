import os

from setuptools import setup, find_packages
setup(
    name="namemodules",
    version="0.1",
    description="Name Distribution Package",
    packages=find_packages(),
    include_package_data=True
)